import 'package:flutter/material.dart';

class AppColors {
  // PRIMARY COLORS
  static const primaryGreen = Color(0xFF66B2B2); // your main green
  static const accentGreen = Color(0xFF22C55E); // secondary green
  static const darkGreen = Color(0xFF0F3D2E); // deep green for dark mode

  // LIGHT MODE COLORS
  static const lightBg = Color(0xFFF5F7F6); // scaffold background light
  static const white = Color(0xFFFFFFFF);
  static const textDark = Color(0xFF1F2937); // primary text
  static const textGrey = Color(0xFF6B7280); // muted text

  // DARK MODE COLORS
  static const darkBg = Color(0xFF071A12); // scaffold background dark
  static const darkSurface = Color(0xFF0F2E22); // card / input background
  static const textLight = Color(0xFFE5E7EB); // light text
  static const accentNeon = Color(0xFF00FFC6); // highlight / neon for dark mode

  // ALERT COLORS
  static const danger = Color(0xFFEF4444);
  static const warning = Color(0xFFF59E0B);

  // EXTRA / UTILITIES
  static const blackColor = Color(0xFF000000); // used in old theme
  static const mutedText = Color(0xFFB0B3B8);
  static const Color greyColor = Color(0xFF9E9E9E);
  static const Color shadowColor = Color(0x33000000);
  static const Color greenColor = Color(
    0xFF4CAF50,
  ); // optional muted text in dark
}
